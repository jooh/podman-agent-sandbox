[machine]
# Bootstrap appends a single dedicated host share to a temporary copy of this
# file. On Podman 5.8.1 for macOS, an empty `volumes = []` machine generated a
# broken Ignition config locally and never finished booting.

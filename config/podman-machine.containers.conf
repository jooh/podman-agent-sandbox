[machine]
# Bootstrap appends a single dedicated host share to a temporary copy of this
# file. Use `scripts/diagnose-podman-machine-nomount` to compare that
# supported one-share baseline against a zero-mount scratch machine.

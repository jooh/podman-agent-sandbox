[machine]
# Bootstrap appends `volumes = []` to a temporary copy of this file so the
# supported machine baseline has zero configured host shares. Use
# `scripts/diagnose-podman-machine-lifecycle` to compare zero-mount against
# one-share and Podman-default scratch machines when investigating lifecycle
# issues.
